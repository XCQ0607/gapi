# ===================================
# AIS2API 配置示例
# ===================================

# HTTP 服务端口
PORT=7860

# 监听地址
HOST=0.0.0.0

# WebSocket 端口（内部通信）
WS_PORT=9998

# API 密钥（多个用逗号分隔）
API_KEYS=your-api-key-1,your-api-key-2

# 流式模式: real 或 fake
STREAMING_MODE=real

# === CFspider 代理配置 ===
# CFspider 服务端点（留空则禁用）
CFSPIDER_ENDPOINT=https://cfspider.web3.dpdns.org

# CFspider 认证 Token（可选，取决于你的 cfspider 配置）
CFSPIDER_TOKEN=

# === 账号切换配置 ===
# 连续失败多少次后切换账号（0=禁用）
FAILURE_THRESHOLD=3

# 使用多少次后自动切换账号（0=禁用）
SWITCH_ON_USES=40

# 遇到这些状态码时立即切换账号（逗号分隔）
IMMEDIATE_SWITCH_STATUS_CODES=429,503

# === 请求重试配置 ===
# 单个请求最多重试次数
MAX_RETRIES=1

# 重试间隔（毫秒）
RETRY_DELAY=2000

# === 浏览器配置 ===
# 浏览器可执行文件路径（留空使用默认）
CAMOUFOX_EXECUTABLE_PATH=

# 初始启动使用的账号索引
INITIAL_AUTH_INDEX=1

# === 认证源配置 ===
# 如果使用环境变量模式，在这里定义认证 JSON
# AUTH_JSON_1={"cookies":[...],"origins":[...]}
# AUTH_JSON_2={"cookies":[...],"origins":[...]}
